#include <iostream>
#include <vector>
using namespace std;
typedef long long int lint;
vector<int> pos_values = {14, 8, 5, 1};

vector<lint> d;

void strip(lint k)
{
	if(k < 0) return n;
}

int main()
{
	lint n;
	cin >> n;
	while(n != -1){
		d = vector<lint> (500001, n);
		
		cout << strip(n, 0) << endl;

		cin >> n;
	}
}